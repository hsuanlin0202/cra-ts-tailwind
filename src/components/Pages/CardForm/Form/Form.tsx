import { CardInfoTypes } from "types/CardForm";

type Props = {
  cardInfo: CardInfoTypes;
  setCardInfo: (target: string, value: string) => void;
};

export const Form = ({ cardInfo, setCardInfo }: Props) => {
  return <>1234</>;
};
